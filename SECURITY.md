# Security

ThreadRelink is local-only and does not include telemetry or a network service.
It reads Codex conversation metadata through the Codex app-server interface and
stores a small project/thread index under `~/.threadrelink`.

Please do not attach `registry.json`, Codex transcripts, or unredacted absolute
paths to public issues. Report vulnerabilities privately to the repository
owner.
